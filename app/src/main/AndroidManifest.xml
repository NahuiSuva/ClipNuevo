<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.informatica.tutorialfirebase">

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Clip"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".TagAdapter"></activity>
        <activity android:name=".RecursoAdapter" />
        <activity android:name=".fragMostrarRecursos" />
        <activity android:name=".fragMostrarTags" />
        <activity
            android:name=".ValoracionActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".fragMostrarEventosDef" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AgregarEditar" />
        <activity
            android:name=".MainActivity"
            android:label="Clip"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>